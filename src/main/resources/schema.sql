-- DDL to create REGISTRATION table
CREATE TABLE REGISTRATION (
                              ID INT PRIMARY KEY,
                              NAME VARCHAR(100) NOT NULL,
                              EMAIL VARCHAR(100) NOT NULL,
                              SHIRT_SIZE VARCHAR(5)
);